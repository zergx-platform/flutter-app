org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin daemon must inherit the same modest heap as the gradle daemon; the
# default 8G would exceed the 8GiB pod cgroup and OOM-kill the build.
kotlin.daemon.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m
# walk through mihomo (JDK + Kotlin daemon both honour the system props)
systemProp.http.proxyHost=mihomo.develop.svc.cluster.local
systemProp.http.proxyPort=7890
systemProp.https.proxyHost=mihomo.develop.svc.cluster.local
systemProp.https.proxyPort=7890
systemProp.http.nonProxyHosts=localhost|127.0.0.1|*.svc.cluster.local|svc.cluster.local|*.nip.io|10.199.64.20
